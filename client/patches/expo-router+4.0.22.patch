diff --git a/node_modules/expo-router/build/fork/NavigationContainer.js b/node_modules/expo-router/build/fork/NavigationContainer.js
index 5e8ae73..85d1071 100644
--- a/node_modules/expo-router/build/fork/NavigationContainer.js
+++ b/node_modules/expo-router/build/fork/NavigationContainer.js
@@ -31,8 +31,13 @@ globalThis.REACT_NAVIGATION_DEVTOOLS = new WeakMap();
  */
 function NavigationContainerInner({ direction = react_native_1.I18nManager.getConstants().isRTL ? 'rtl' : 'ltr', theme = native_1.DefaultTheme, linking, fallback = null, documentTitle, onReady, onStateChange, ...rest }, ref) {
     const isLinkingEnabled = linking ? linking.enabled !== false : false;
-    if (linking?.config) {
-        (0, native_1.validatePathConfig)(linking.config);
+    if (linking?.config && typeof linking.config === 'object') {
+        try {
+            (0, native_1.validatePathConfig)(linking.config);
+        }
+        catch (error) {
+            console.warn('Ignoring invalid linking config to avoid startup crash:', error);
+        }
     }
     const refContainer = react_1.default.useRef(null);
     (0, useBackButton_1.useBackButton)(refContainer);
diff --git a/node_modules/expo-router/build/fork/getPathFromState.js b/node_modules/expo-router/build/fork/getPathFromState.js
index b2c02bc..18becd0 100644
--- a/node_modules/expo-router/build/fork/getPathFromState.js
+++ b/node_modules/expo-router/build/fork/getPathFromState.js
@@ -104,7 +104,7 @@ function getPathDataFromState(state, options) {
         // Keep all the route names that appeared during going deeper in config in case the pattern is resolved to undefined
         const nestedRouteNames = [];
         let hasNext = true;
-        while (route.name in currentOptions && hasNext) {
+        while (currentOptions && typeof currentOptions === 'object' && typeof route?.name === 'string' && route.name in currentOptions && hasNext) {
             pattern = currentOptions[route.name].pattern;
             nestedRouteNames.push(route.name);
             if (route.params) {
@@ -161,8 +161,8 @@ function getPathDataFromState(state, options) {
                 // Determine what screen the user wants to navigate to. If no screen is specified, assume there is an index screen
                 // In the examples above, this ensures that /home/(a) navigates to /home/(a)/index
                 const targetScreen =
-                // This is typed as unknown, so we need to add these extra assertions
-                route.params && 'screen' in route.params && typeof route.params.screen === 'string'
+                // route.params can be a truthy primitive (e.g. string), so guard object type before using `in`
+                route.params && typeof route.params === 'object' && 'screen' in route.params && typeof route.params.screen === 'string'
                     ? route.params.screen
                     : 'index';
                 // If the target screen is not in the screens object, default to the first screen
@@ -189,7 +189,7 @@ function getPathDataFromState(state, options) {
                 const nextRoute = route.state.routes[index];
                 const nestedConfig = currentOptions[route.name].screens;
                 // if there is config for next route name, we go deeper
-                if (nestedConfig && nextRoute.name in nestedConfig) {
+                if (nestedConfig && typeof nestedConfig === 'object' && typeof nextRoute?.name === 'string' && nextRoute.name in nestedConfig) {
                     route = nextRoute;
                     currentOptions = nestedConfig;
                 }
